# 更新日志

本项目采用接近语义化版本的版本号；稳定前使用 Alpha/Beta 标记。

## [0.3.0-alpha01 / v0.3-test] - 2026-07-30

### Android 原生迁移

- 将 Android 应用主体从 Capacitor/WebView HTML 外壳迁移为 Kotlin + Jetpack Compose 原生界面。
- 新增首页、配方浏览、搜索、中英文切换、配方详情、收藏和今夜酒单。
- 使用 SharedPreferences 保存语言、收藏和今夜酒单。
- Web 配方目录通过构建期脚本导出为原生 `catalog.json`，保持材料和配方 ID 稳定。
- 最低支持 Android 7.0（API 24），目标 API 36；版本更新为 `versionCode 3`、`versionName 0.3.0-alpha01`。

### 自适应、视觉与动效

- 窄屏使用底部导航，大屏使用 Navigation Rail；配方列表使用自适应网格。
- 首页统计卡、详情操作按钮和文字布局针对窄屏自动重排。
- 新增暮色紫、酒红、黎明橙与金色高光品牌色板。
- 使用 Compose Canvas 绘制杯型、酒液、装饰、环境光晕和首页主视觉。
- 新增页面切换、卡片分批入场、按压反馈、收藏/今夜状态和环境光动画。
- 读取 Android 动画缩放设置，系统关闭动画时同步减少自定义动效。
- Web 新增 Hero 光晕、酒杯悬浮、卡片入场、弹窗开闭和按钮反馈，并尊重 `prefers-reduced-motion`。

### Web 响应式与可访问性

- 增加 `dvh` 高度回退、移动表单 16px 字号、窄屏底部导航文字约束和触屏 hover 降级。
- 完善配方弹窗焦点进入、焦点循环、Escape 关闭、关闭后焦点恢复及 `aria-hidden` 同步。
- 配方卡片仅在集合发生变化时重新播放入场动画，避免收藏变化触发全列表闪动。

### 工程与验证

- 新增原生目录导出与项目完整性检查工具。
- 新增本地化模型单元测试。
- Debug APK 可完成编译和打包；正式发布仍需私有 release 签名、AAB 验证和设备验收。

### 当前限制

- 原生 Alpha 尚未实现酒柜、完整 ABV/成本计算、自定义配方、JSON 导入导出、随机/聚会推荐等 Web v0.2 功能。
- 旧 Capacitor 用户数据位于 WebView localStorage，当前不会自动迁移到原生 SharedPreferences。
- 在完成数据迁移、签名连续性、功能取舍和覆盖升级测试前，不应将本版本作为正式覆盖更新。
## [0.2-beta] - 2026-07-27

- 将个人开发版本格式由三段式 `v0.0.2` 简化为 `v0.2`，功能与数据继续兼容旧版。

### 新增

- 产品中文名称更新为“朝露酒笺”，英文名称继续使用 “Dawn's Dew”。
- 新增“今夜酒单 / Tonight's Menu”：可从内置或自定义配方中自由加入、移除和清空。
- 首页新增今夜酒单预览，最多展示 4 杯候选配方。
- 桌面侧栏、配方卡片、配方详情与手机底部导航均可访问今夜酒单。
- 新增 Capacitor Android 工程，应用 ID 为 `com.zalandunk.dawnsdew`。
- 新增 Android 图标、启动页、离线资源与返回键导航处理。
- Android 版导出 JSON 时写入缓存并调起系统分享面板。
- 新增移动端资源构建脚本与 v0.2 单文件封装版。
- 新增 PWA 安装支持：提供 Web App Manifest、Service Worker、离线应用外壳与金黑主题桌面图标。
- 新增 iPhone Safari“添加到主屏幕”引导，安装后可从桌面全屏启动。
- 新增独立的 iPhone PWA 安装与数据说明文档。

### 改进

- 本地存储升级为 `dawnsdew.v0.2.state`，并新增 `tonightMenu` 数据。
- 自动迁移 v0.0.1 的酒柜、收藏、自定义配方、最近查看和聚会历史。
- v0.0.1 JSON 备份继续兼容导入。
- 自定义配方被删除时，会同步从今夜酒单移除。
- 手机底部导航扩展为 6 个入口并继续适配安全区域。
- 构建环境支持 Android SDK 36、最低 API 24 和 JDK 21。
- PWA 注册仅在 HTTPS 或本地开发服务器上启用，不影响 `file://` 单文件版与 Capacitor 原生容器。
- 单文件封装构建会自动移除仅适用于在线托管环境的 PWA 元数据与安装界面。

### Android 测试包

- `versionCode`: `2`
- `versionName`: `0.2-beta`
- 最低系统：Android 7.0（API 24）
- 当前提供 debug APK，适合直接安装测试；正式发布需使用私有密钥签名 release 包。

## [0.0.1-beta] - 2026-07-25

### 新增

- 建立“破晓微醺 / Dawn's Dew”本地静态网页版。
- 内置 25 杯个人配方、15 杯经典配方和 5 杯便利店灵感配方。
- 支持中英文界面和双语配方内容。
- 支持名称、英文名、材料搜索与来源、基酒、口味、难度筛选。
- 支持个人酒柜、材料拥有状态、库存、价格、包装容量和酒精度记录。
- 支持按已有材料识别可制作配方和缺少材料。
- 支持收藏、最近查看、普通随机推荐和聚会防重复推荐。
- 支持自定义配方。
- 支持本地自动保存、版本化 JSON 导出与导入。
- 支持酒精度、酒液容量和成本估算。
- 支持桌面端和移动端响应式布局。
- 新增可直接分享的单 HTML 封装版，内置全部美术、样式、数据与程序脚本。
- 优化手机端安全区域、底部导航、触控尺寸、酒柜字段与窄屏布局。
- 增加旧版移动浏览器兼容层与脚本启动诊断提示，便于识别禁用 JavaScript 的文件预览环境。

### 说明

- “八分满”等杯量表达按有效酒液容量计算。
- 默认 300 ml 杯具；满冰时默认有效酒液容量为 150 ml。
- 保留用户个人配方名称、商品名称、评级与原始记录。
- “血腥玛丽”和“自由古巴”明确标注为破晓特调版。
