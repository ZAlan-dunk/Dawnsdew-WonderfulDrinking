# 更新日志

本项目采用接近语义化版本的版本号；稳定前使用 Alpha/Beta 标记。

## [0.3.3-beta01 / v0.3.3-beta] - 2026-07-31

### 紧凑配方与酒柜浏览

- 配方页把常驻搜索框改为 40dp 搜索图标，点击后打开自动聚焦的顶部悬浮搜索栏。
- 配方标题、搜索、快速条件和详细筛选合并为单行紧凑工具区，减少浏览列表前的固定占高。
- 酒柜移除大统计卡、重复说明与列表内联编辑，改为紧凑状态行和顶部横向品类滑栏；一次只呈现当前品类。
- 酒柜条目压缩到约 78dp，展示名称、相关配方数、ABV 与拥有状态，库存编辑移入材料详情。

### 酒款视觉与内容

- 为 18 种含酒精材料建立原创瓶型图标，依据常见实体瓶的圆肩、方瓶、高直瓶、药剂瓶、起泡酒瓶和苦精标签等轮廓特征绘制，不嵌入商标 Logo 或产品照片。
- 新增材料详情，包含中英文介绍、起源、至少两个发展节点、常见品牌风味线索、相关经典配方与库存编辑。
- 点击相关配方可直接进入完整配方详情；非酒精材料按品类提供介绍与历史回退内容。

### 动效与性能

- 首页诗意文字区域增加低密度漂移粒子，按钮增加短距离粒子反馈。
- 动效尊重系统“减少动态效果”设置；低内存、低核心数或省流量设备关闭持续粒子，Android 低内存设备继续停用环境动画。
- Web/PWA 同步紧凑搜索、分类酒柜、瓶型图标、材料故事与粒子反馈，继续支持 iPhone 添加到主屏幕。

### 工程

- Android 版本更新为 `versionCode 6`、`versionName 0.3.3-beta01`，状态升级到 `0.3.3` 并自动迁移 `state.v0.3.2` 与 `state.v0.3.1`。
- npm 版本更新为 `0.3.3-beta.1`，PWA 离线缓存更新为 `dawnsdew-pwa-v0.3.3-beta`。
- 新增酒款故事覆盖与历史节点测试。

## [0.3.2-beta01] - 2026-07-30

### 主题、排版与控件

- 新增朝露白、夜酿墨和跟随系统三种主题模式，亮色主题成为默认外观。
- 新增黎明金、珊瑚红和月光青绿三种强调色，并将界面颜色迁移为语义角色。
- 新增 90%–130% 应用字体缩放，继续叠加 Android 系统字体缩放。
- 统一输入框、选择器、筛选标签、工具按钮和弹窗的圆角层级；移除更多页无必要的前进箭头。
- 原生状态升级到 0.3.2，并自动迁移 `state.v0.3.1`，外观设置随 Web v0.2 兼容 JSON 一起备份。

### 配方浏览与品牌内容

- 配方页移除重复标题、副标题和独立结果提示，改为 48dp 搜索栏、快速筛选和底部详细筛选抽屉。
- 350dp 以上常见手机默认双列紧凑卡片；窄屏或大字体自动回退单列。
- 手机配方详情改为全屏，平板保留居中大尺寸详情。
- 为目录中的 18 种含酒精材料建立品牌与风味资料；卡片显示首选品牌，详情显示最多三种风味替代。
- 品牌视觉仅使用文字和原创程序化酒杯/瓶型表达，不嵌入未经许可的 Logo 或产品照片。

### Web/PWA

- PWA 同步主题、强调色、字体缩放、紧凑双列配方和折叠详细筛选。
- 更新 PWA 主题色和离线缓存版本，iPhone 主屏幕模式继续保持本地优先与离线可用。

### 工程

- Android 版本更新为 `versionCode 5`、`versionName 0.3.2-beta01`。
- 新增亮暗主题对比度和酒类品牌覆盖测试。

## [0.3.1-beta01 / v0.3.1-beta] - 2026-07-30

### 视觉与首页叙事

- 修复深色界面中 Hero 标题、统计数字和章节标题继承黑色内容色的问题。
- 重新定义墨色、暖白、黎明金、珊瑚色、酒红和月光青绿色语义色，并增加核心阅读组合的对比度单元测试。
- 首页主叙事调整为“把今夜的心绪，斟成一杯”，围绕今夜酒单、收藏与举杯时刻展开，不再以便利店材料作为首页卖点。
- 重做首页为无边框夜色视觉带，强化原生杯型、酒液、光环和今夜酒单预览。
- 字体层级改为标题适度使用衬线、正文与操作使用高可读无衬线；所有字距归零。

### 原生功能对等

- 新增完整酒柜：拥有状态、库存、购入价、包装容量和实际酒精度。
- 新增可制作、差一种和缺少多种材料匹配，并支持从详情快速把缺少材料加入酒柜。
- 新增酒精度、酒液容量、成本覆盖率和单杯成本估算。
- 新增来源、基酒、口味、难度、可制作和收藏筛选。
- 新增普通随机、聚会优先可制作及最近 12 杯防重复推荐。
- 新增最近查看、自定义配方创建/删除、今夜酒单清空和完整本地数据重置。
- 新增 Android Storage Access Framework JSON 导入导出，支持导入 Web v0.2 备份，并导出 Web v0.2 兼容数据。
- 将语言、收藏、今夜酒单、酒柜、自定义配方、设置和历史统一迁移到版本化原生状态。

### 动效与性能

- 首页和页面增加可见的短促淡入/位移动效，卡片使用分批入场，收藏与今夜操作使用颜色、缩放和图标切换反馈。
- 移除全屏持续重绘的环境漂移动画；持续动画仅保留在小面积杯型画布。
- 低内存设备自动关闭持续环境动画，LazyColumn/LazyVerticalGrid 仅组合可见项目。
- 保留系统动画缩放为 0 时关闭自定义动效的行为。

### 工程与验证

- Android 版本更新为 `versionCode 4`、`versionName 0.3.1-beta01`。
- 拆分原生状态、计算、JSON 编解码、首页、配方、酒柜、更多与数据管理模块。
- 新增配方估算、材料匹配、搜索规范化和主题对比度测试。

## [0.3.0-alpha01 / v0.3-test] - 2026-07-30

### Android 原生迁移

- 将 Android 应用主体从 Capacitor/WebView HTML 外壳迁移为 Kotlin + Jetpack Compose 原生界面。
- 新增首页、配方浏览、搜索、中英文切换、配方详情、收藏和今夜酒单。
- 使用 SharedPreferences 保存语言、收藏和今夜酒单。
- Web 配方目录通过构建期脚本导出为原生 `catalog.json`，保持材料和配方 ID 稳定。
- 最低支持 Android 7.0（API 24），目标 API 36；版本更新为 `versionCode 3`、`versionName 0.3.0-alpha01`。

### 自适应、视觉与动效

- 窄屏使用底部导航，大屏使用 Navigation Rail；配方列表使用自适应网格。
- 首页统计卡、详情操作按钮和文字布局针对窄屏自动重排。
- 新增暮色紫、酒红、黎明橙与金色高光品牌色板。
- 使用 Compose Canvas 绘制杯型、酒液、装饰、环境光晕和首页主视觉。
- 新增页面切换、卡片分批入场、按压反馈、收藏/今夜状态和环境光动画。
- 读取 Android 动画缩放设置，系统关闭动画时同步减少自定义动效。
- Web 新增 Hero 光晕、酒杯悬浮、卡片入场、弹窗开闭和按钮反馈，并尊重 `prefers-reduced-motion`。

### Web 响应式与可访问性

- 增加 `dvh` 高度回退、移动表单 16px 字号、窄屏底部导航文字约束和触屏 hover 降级。
- 完善配方弹窗焦点进入、焦点循环、Escape 关闭、关闭后焦点恢复及 `aria-hidden` 同步。
- 配方卡片仅在集合发生变化时重新播放入场动画，避免收藏变化触发全列表闪动。

### 工程与验证

- 新增原生目录导出与项目完整性检查工具。
- 新增本地化模型单元测试。
- Debug APK 可完成编译和打包；正式发布仍需私有 release 签名、AAB 验证和设备验收。

### 当前限制

- 原生 Alpha 尚未实现酒柜、完整 ABV/成本计算、自定义配方、JSON 导入导出、随机/聚会推荐等 Web v0.2 功能。
- 旧 Capacitor 用户数据位于 WebView localStorage，当前不会自动迁移到原生 SharedPreferences。
- 在完成数据迁移、签名连续性、功能取舍和覆盖升级测试前，不应将本版本作为正式覆盖更新。
## [0.2-beta] - 2026-07-27

- 将个人开发版本格式由三段式 `v0.0.2` 简化为 `v0.2`，功能与数据继续兼容旧版。

### 新增

- 产品中文名称更新为“朝露酒笺”，英文名称继续使用 “Dawn's Dew”。
- 新增“今夜酒单 / Tonight's Menu”：可从内置或自定义配方中自由加入、移除和清空。
- 首页新增今夜酒单预览，最多展示 4 杯候选配方。
- 桌面侧栏、配方卡片、配方详情与手机底部导航均可访问今夜酒单。
- 新增 Capacitor Android 工程，应用 ID 为 `com.zalandunk.dawnsdew`。
- 新增 Android 图标、启动页、离线资源与返回键导航处理。
- Android 版导出 JSON 时写入缓存并调起系统分享面板。
- 新增移动端资源构建脚本与 v0.2 单文件封装版。
- 新增 PWA 安装支持：提供 Web App Manifest、Service Worker、离线应用外壳与金黑主题桌面图标。
- 新增 iPhone Safari“添加到主屏幕”引导，安装后可从桌面全屏启动。
- 新增独立的 iPhone PWA 安装与数据说明文档。

### 改进

- 本地存储升级为 `dawnsdew.v0.2.state`，并新增 `tonightMenu` 数据。
- 自动迁移 v0.0.1 的酒柜、收藏、自定义配方、最近查看和聚会历史。
- v0.0.1 JSON 备份继续兼容导入。
- 自定义配方被删除时，会同步从今夜酒单移除。
- 手机底部导航扩展为 6 个入口并继续适配安全区域。
- 构建环境支持 Android SDK 36、最低 API 24 和 JDK 21。
- PWA 注册仅在 HTTPS 或本地开发服务器上启用，不影响 `file://` 单文件版与 Capacitor 原生容器。
- 单文件封装构建会自动移除仅适用于在线托管环境的 PWA 元数据与安装界面。

### Android 测试包

- `versionCode`: `2`
- `versionName`: `0.2-beta`
- 最低系统：Android 7.0（API 24）
- 当前提供 debug APK，适合直接安装测试；正式发布需使用私有密钥签名 release 包。

## [0.0.1-beta] - 2026-07-25

### 新增

- 建立“破晓微醺 / Dawn's Dew”本地静态网页版。
- 内置 25 杯个人配方、15 杯经典配方和 5 杯灵感特调。
- 支持中英文界面和双语配方内容。
- 支持名称、英文名、材料搜索与来源、基酒、口味、难度筛选。
- 支持个人酒柜、材料拥有状态、库存、价格、包装容量和酒精度记录。
- 支持按已有材料识别可制作配方和缺少材料。
- 支持收藏、最近查看、普通随机推荐和聚会防重复推荐。
- 支持自定义配方。
- 支持本地自动保存、版本化 JSON 导出与导入。
- 支持酒精度、酒液容量和成本估算。
- 支持桌面端和移动端响应式布局。
- 新增可直接分享的单 HTML 封装版，内置全部美术、样式、数据与程序脚本。
- 优化手机端安全区域、底部导航、触控尺寸、酒柜字段与窄屏布局。
- 增加旧版移动浏览器兼容层与脚本启动诊断提示，便于识别禁用 JavaScript 的文件预览环境。

### 说明

- “八分满”等杯量表达按有效酒液容量计算。
- 默认 300 ml 杯具；满冰时默认有效酒液容量为 150 ml。
- 保留用户个人配方名称、商品名称、评级与原始记录。
- “血腥玛丽”和“自由古巴”明确标注为破晓特调版。
